---
bundle_name: internal
description: Trust bundle for internal PKI services (root + issuing CAs)
repo:
  - name: internal
    include: [sources/internal/rootCA.pem, sources/internal/issuingCA1.pem]
metadata:
  owner: pki-team@example.com
  purpose: Internal service authentication and TLS
  tags: [internal, production]
